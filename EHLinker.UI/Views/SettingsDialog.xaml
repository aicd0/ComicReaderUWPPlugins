<ContentDialog
    x:Class="EHLinker.UI.Views.SettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="EHLinker Settings"
    mc:Ignorable="d">

    <StackPanel Width="600" Orientation="Vertical">
        <CheckBox
            Click="ImportTagsAutomaticallyCheckBox_Click"
            Content="Import tags automatically"
            IsChecked="{x:Bind ViewModel.ImportTagsAutomatically, Mode=OneWay}" />
        <Button
            Margin="0,6,0,0"
            HorizontalAlignment="Stretch"
            Click="EditCookiesButton_Click"
            Content="Edit cookies..." />
        <Grid Margin="0,15,0,0" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button
                Grid.Column="2"
                HorizontalAlignment="Stretch"
                Click="CloseButton_Click"
                Content="Close" />
        </Grid>
    </StackPanel>

</ContentDialog>

